{
  "name": "takanawa",
  "version": "0.5.0",
  "private": true,
  "packageManager": "pnpm@11.6.0",
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "build:js-core": "pnpm --filter takanawa-js-core build",
    "build:node": "pnpm --filter takanawa-node build",
    "build:capacitor": "pnpm --filter takanawa-capacitor build",
    "build:tauri": "pnpm --filter takanawa-tauri build"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
