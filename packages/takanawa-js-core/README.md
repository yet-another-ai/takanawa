# takanawa-js-core

Shared TypeScript facade and target adapter contracts for Takanawa npm packages.

This package is used by target packages such as `takanawa-node` and
`takanawa-capacitor`. Application code should normally install one of those
target packages instead of depending on this package directly.
